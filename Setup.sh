echo Thanks for downloading, this file will setup what you need to have done.

chmod 777 LetsLearn
# This makes the file lets learn ecxecutable

export PATH=$PATH:.
# This turns it into a bash command
